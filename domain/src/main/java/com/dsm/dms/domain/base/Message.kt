package com.dsm.dms.domain.base

enum class Message {
    OK,
    NO_CONTENT,
    INVALID,
    UNAUTHORIZED,
    FORBIDDEN,
    NOT_FOUND,
    CONFLICT,
    UNKNOW_ERROR,
    SERVER_ERROR,
    NETWORK_ERROR
}